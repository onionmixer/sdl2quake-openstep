# Build output goes to the target's own filesystem, never here.
build-output/
release-assets/
*.o

# Built Installer packages stay out of the tree too.
build/release-pkgs/

# Internal handoff; operational detail that does not belong in a clone.
HANDOFF_SDL2QUAKE.md
